services:
  redops:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/redops.db
    command: ["serve", "--host", "0.0.0.0", "--port", "8000"]
